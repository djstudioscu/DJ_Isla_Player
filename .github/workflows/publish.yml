name: Release App (EXE)

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-release:
    # ESENCIAL: Debe decir windows-latest para evitar el error NETSDK1100
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore ./VideoPlayerApp/VideoPlayerApp.csproj

      - name: Publish (Generar EXE único)
        # Creamos un ejecutable que contiene todo lo necesario
        run: |
          dotnet publish ./VideoPlayerApp/VideoPlayerApp.csproj `
            -c Release `
            -o ./publish `
            --runtime win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:PublishReadyToRun=true `
            -p:IncludeNativeLibrariesForSelfExtract=true

      - name: Zip Release
        # Comprimimos la carpeta para que el usuario descargue un ZIP
        run: Compress-Archive -Path ./publish/* -DestinationPath ./DJ_Isla_Player_v${{ github.ref_name }}.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: ./DJ_Isla_Player_v${{ github.ref_name }}.zip
          name: Release ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          draft: false
          prerelease: false
        env:
<<<<<<< HEAD
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
=======
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
>>>>>>> da6e110081d8c1803517e9ccdc036a26b7b825c9
